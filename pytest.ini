[pytest]
testpaths = day1_single_agent day2_pipeline day3_delegation
markers =
    integration: makes a real call through the ADK Runner (costs tokens, can be slow/flaky). Run explicitly with `pytest -m integration`.
addopts = -m "not integration"
